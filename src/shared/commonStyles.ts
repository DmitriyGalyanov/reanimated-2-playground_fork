import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  screenWrap: { flex: 1, backgroundColor: 'gray' },

  flex1: { flex: 1 },

  fillParent: { width: '100%', height: '100%' },
});

export const buttonTextColor = 'black';
